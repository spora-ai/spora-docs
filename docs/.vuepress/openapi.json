{
  "openapi": "3.0.0",
  "info": {
    "title": "Spora API (stub)",
    "version": "0.0.0",
    "description": "Placeholder spec shipped with the widdershins pipeline. Replaced by the real `spora:openapi` artifact after spora-core PR #140 merges — see `scripts/fetch-spec.mjs`."
  },
  "servers": [
    { "url": "/", "description": "Same-origin" }
  ],
  "paths": {
    "/api/v1/auth/login": {
      "post": {
        "tags": ["Auth"],
        "summary": "Authenticate (issues data.csrf_token)",
        "description": "Placeholder operation; real operations land after spora-core PR #140 merges.",
        "security": [],
        "parameters": [],
        "responses": {
          "default": {
            "description": "JSON envelope: `{data: ...}` on success, `{error: {code, message}}` on error."
          }
        }
      }
    },
    "/api/v1/auth/logout": {
      "post": {
        "tags": ["Auth"],
        "summary": "End session",
        "security": [{ "csrfToken": [] }],
        "parameters": [],
        "responses": {
          "default": {
            "description": "JSON envelope."
          }
        }
      }
    }
  },
  "components": {
    "securitySchemes": {
      "cookieAuth": {
        "type": "apiKey",
        "in": "cookie",
        "name": "PHPSESSID",
        "description": "Session cookie issued by `delight-im/auth`."
      },
      "csrfToken": {
        "type": "apiKey",
        "in": "header",
        "name": "X-CSRF-Token",
        "description": "CSRF token issued alongside the session."
      }
    }
  }
}
